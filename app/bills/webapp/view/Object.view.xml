<mvc:View
    controllerName="ns.bills.controller.Object"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:semantic="sap.f.semantic">

    <semantic:SemanticPage
        id="page"
        headerPinnable="false"
        toggleHeaderOnTitleClick="false"
        busy="{objectView>/busy}"
        busyIndicatorDelay="{objectView>/delay}">

        <semantic:titleHeading>
            <Title
                text="{ID}"
                level="H2"/>
        </semantic:titleHeading>

        <semantic:headerContent>
            <ObjectNumber />
        </semantic:headerContent>

        <semantic:sendEmailAction>
            <semantic:SendEmailAction id="shareEmail" press=".onShareEmailPress"/>
        </semantic:sendEmailAction>

         <semantic:content>
            <l:VerticalLayout>
                <ObjectStatus title="ID" text="{ID}"/>
                <ObjectStatus title="customer" text="{customer}"/>
                <ObjectStatus title="exporter" text="{exporter}"/>
                <ObjectStatus title="createdAt" text="{createdAt}"/>
                <ObjectStatus title="items.name" text="{items.name}"/>
                <ObjectStatus title="total" text="{total}"/>
            </l:VerticalLayout>
        </semantic:content>

    </semantic:SemanticPage>

</mvc:View>