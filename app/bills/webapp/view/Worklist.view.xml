<mvc:View
    controllerName="ns.bills.controller.Worklist"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:semantic="sap.f.semantic">

    <semantic:SemanticPage
        id="page"
        headerPinnable="false"
        toggleHeaderOnTitleClick="false">

        <semantic:titleHeading>
            <Title
                text="{i18n>worklistTitle}"
                level="H2"/>
        </semantic:titleHeading>

        <semantic:content>
            <Table
                id="table"
                width="auto"
                items="{
                    path: '/Bills',
                    sorter: {
                        path: 'ID',
                        descending: false
                    }
                }"
                noDataText="{worklistView>/tableNoDataText}"
                busyIndicatorDelay="{worklistView>/tableBusyDelay}"
                growing="true"
                growingScrollToLoad="true"
                updateFinished=".onUpdateFinished">

                <headerToolbar>
                    <OverflowToolbar>
                        <Title
                            id="tableHeader"
                            text="{worklistView>/worklistTableTitle}"
                            level="H3"/>
                        <ToolbarSpacer />
                        <SearchField
                            id="searchField"
                            tooltip="{i18n>worklistSearchTooltip}"
                            search=".onSearch">
                            <layoutData>
                                <OverflowToolbarLayoutData
                                    maxWidth="200px"
                                    priority="NeverOverflow"/>
                            </layoutData>
                        </SearchField>
                    </OverflowToolbar>
                </headerToolbar>

                <columns>
                    <Column id="IDColumn">
                        <Text text="ID" id="IDColumnTitle"/>
                    </Column>
                    <Column id="CustomerColumn">
                        <Text text="Customer" id="CustomerColumnTitle"/>
                    </Column>
                    <Column id="ExporterColumn">
                        <Text text="Exporter" id="ExporterColumnTitle"/>
                    </Column>
                    <Column id="CreatedAtColumn">
                        <Text text="CreatedAt" id="CreatedAtColumnTitle"/>
                    </Column>
                    <Column id="ItemColumn">
                        <Text text="Item" id="ItemColumnTitle"/>
                    </Column>
                    <Column id="TotalColumn" hAlign="End">
                        <Text text="Total" id="TotalColumnTitle"/>
                    </Column>
                </columns>

                <items>
                    <ColumnListItem
                        type="Navigation"
                        press=".onPress">
                        <cells>
                             <ObjectIdentifier
                                text="{ID}" />
                            <Text
                                text="{customer}" />
                            <Text
                                text="{exporter}" />
                            <Text
                                text="{createdAt}" />
                            <Text
                                text="{items/0/item/name}" />
                            <Text
                                text="{total}" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </semantic:content>

        <semantic:sendEmailAction>
            <semantic:SendEmailAction id="shareEmail" press=".onShareEmailPress"/>
        </semantic:sendEmailAction>

    </semantic:SemanticPage>

</mvc:View>